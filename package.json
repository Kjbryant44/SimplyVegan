{
  "name": "vgrecipe",
  "version": "1.0.0",
"scripts": {
  "start": "node server.js",
  "heroku-postbuild": "cd frontend && npm install --legacy-peer-deps && npm run build"
},
  "engines": {
    "node": "20.x"
  }
}